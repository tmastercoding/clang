#include <stdio.h>

int main(){
    // the number we are checking if its prime or not

    int guess = 5;
    int prime_arr[1000]; // the array of prime numbers

    int index = 1; // index
    int i;
    int ok; // the number we use to check if guess is prime or not
    prime_arr[0] = 2;
    
    // figure out if number is already in prime arr

    // checks if the number of prime numbers is equal to guess

    // if 1000 prime numbers are discovered breaks



    return 0;
}